const fs = require('fs'); let code = fs.readFileSync('src/app/chat.tsx', 'utf8'); code = code.replace(/const handleSendAlert = useCallback\(\(alertData: any\) => \{[\s\S]*?\}, \[\]\);/, 'const handleSendAlert = useCallback(async (alertData: any) => { if (!id || !user) return; await supabase.from('messages').insert({ chat_id: id as string, sender_id: user.id, content: JSON.stringify(alertData), type: 'alert' }); }, [id, user]);'); fs.writeFileSync('src/app/chat.tsx', code);
