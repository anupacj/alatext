const { createClient } = require('@supabase/supabase-js'); const supabase = createClient('https://ghjnmkjxadzahlfbiacv.supabase.co', 'sb_publishable_XT1WQ9GD8ECesZUOKPtlLw_cbd7ftuW'); async function test() { const { data: p } = await supabase.from('sticker_packs').select('id').limit(1); if (!p || p.length === 0) return console.log('no packs'); const res = await supabase.from('stickers').insert({ pack_id: p[0].id, file_url: 'https://test.com/img.png', emoji: '??' }); console.log(res.error); } test();
