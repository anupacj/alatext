const { createClient } = require('@supabase/supabase-js'); const supabase = createClient('https://ghjnmkjxadzahlfbiacv.supabase.co', 'sb_publishable_XT1WQ9GD8ECesZUOKPtlLw_cbd7ftuW'); async function check() { const { data } = await supabase.from('sticker_packs').select('*'); console.log('Packs:', data.length); const { data: s } = await supabase.from('stickers').select('*'); console.log('Stickers:', s.length); if (s.length > 0) console.log('Sample sticker:', s[0]); } check();
